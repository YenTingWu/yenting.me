import { Inter, Rubik } from 'next/font/google';

const inter = Inter({ subsets: ['latin'], weight: ['400', '600', '700'] });
const rubik = Rubik({
  subsets: ['latin'],
  weight: ['300', '400'],
});

export { inter, rubik };
